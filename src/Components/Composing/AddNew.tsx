import { Box, Typography } from "@mui/material";

const AddNew: React.VFC = () => (
    <Box>
        <Typography color="#1f2">
            This will be a control to allow you to add a new item to the list.
        </Typography>
    </Box>
);

export default AddNew;
