import { Box, Typography } from "@mui/material";

const List = () => (
  <Box>
    <Typography color="secondary">
      * bread * milk * a brace of chainsaws
    </Typography>
  </Box>
);

export default List;
